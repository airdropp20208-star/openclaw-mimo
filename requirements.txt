# Hermes Agent + OpenManus Tools
# Core — stdlib only (no external deps needed for bot.py)
# Optional: markitdown for file conversion, python-pptx for PPT
markitdown[all]>=0.1.0
python-pptx>=0.6.21
browser-use>=0.1.0
playwright>=1.40.0
