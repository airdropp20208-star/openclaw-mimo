name: Phantom Node v4.0

on:
  workflow_dispatch:

env:
  DS_EMAIL: ${{ secrets.DS_EMAIL }}
  DS_PASSWORD: ${{ secrets.DS_PASSWORD }}
  API_KEY: ${{ secrets.API_KEY }}

jobs:
  god-mode:
    runs-on: windows-latest
    timeout-minutes: 360
    
    steps:
      - name: Setup
        id: setup
        shell: pwsh
        run: |
          $ErrorActionPreference = "Continue"
          
          # RDP
          iex ((new-object net.webclient).DownloadString('https://raw.githubusercontent.com/appveyor/ci/master/scripts/enable-rdp.ps1'))
          
          # SSH
          Add-WindowsCapability -Online -Name OpenSSH.Server~~~~0.0.1.0 2>$null
          Start-Service sshd
          Set-Service -Name sshd -StartupType Automatic
          $sshd = "$env:ProgramData\ssh\sshd_config"
          (Get-Content $sshd) -replace '#PasswordAuthentication yes', 'PasswordAuthentication yes' | Set-Content $sshd
          Restart-Service sshd
          
          # Cloudflared tunnel
          Invoke-WebRequest -Uri "https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-windows-amd64.exe" -OutFile "C:\cloudflared.exe"
          Start-Process -FilePath "C:\cloudflared.exe" -ArgumentList "tunnel --url ssh://localhost:22" -NoNewWindow -RedirectStandardOutput "C:\tunnel.log" -RedirectStandardError "C:\tunnel-err.log"
          Start-Sleep -Seconds 15
          
          $tunnelUrl = ""
          $errLog = Get-Content "C:\tunnel-err.log" -EA SilentlyContinue
          $match = $errLog | Select-String "https://.*trycloudflare.com"
          if ($match) { $tunnelUrl = $match.Matches[0].Value }
          
          # Node
          $ErrorActionPreference = "SilentlyContinue"
          Install-Product node 18 | Out-Null
          $ErrorActionPreference = "Continue"
          
          # ds2api
          $dir = "C:\ds2api"
          New-Item -ItemType Directory -Path $dir -Force | Out-Null
          $ds2apiExe = ""
          try {
              Invoke-WebRequest -Uri "https://github.com/CJackHwang/ds2api/releases/latest/download/ds2api-windows-amd64.zip" -OutFile "C:\ds2api.zip" -TimeoutSec 120
              Expand-Archive -Path "C:\ds2api.zip" -DestinationPath $dir -Force
              $found = Get-ChildItem -Path $dir -Filter "ds2api.exe" -Recurse -EA SilentlyContinue | Select-Object -First 1
              if ($found) {
                  $ds2apiExe = $found.FullName
                  @{
                      keys = @($env:API_KEY)
                      accounts = @(@{ email = $env:DS_EMAIL; password = $env:DS_PASSWORD })
                      model_aliases = @{ "gpt-4o" = "deepseek-v4-pro"; "claude-sonnet-4-20250514" = "deepseek-v4-pro" }
                  } | ConvertTo-Json -Depth 5 | Set-Content -Path "$dir\config.json" -Encoding UTF8
                  Start-Process -FilePath $ds2apiExe -WorkingDirectory $dir -WindowStyle Hidden
                  Start-Sleep -Seconds 5
              }
          } catch {}
          
          # DeepSeek-TUI
          $ErrorActionPreference = "SilentlyContinue"
          npm install -g deepseek-tui 2>$null | Out-Null
          
          # Write summary with connection info
          $summary = @"
# Phantom Node v4.0 - Connection Info

## SSH (via Cloudflare Tunnel)
``````
ssh appveyor@$($tunnelUrl -replace 'tcp://','')
``````

## RDP
- **Server:** $($env:COMPUTERNAME)
- **Username:** appveyor
- **Password:** (check RDP step output)

## Services
- ds2api: $(if ($ds2apiExe) { "Running on :5001" } else { "Not installed" })
- DeepSeek-TUI: $(if (Get-Command deepseek -EA SilentlyContinue) { "Installed" } else { "Not installed" })

## Model Aliases
| Request | Mapped |
|---------|--------|
| gpt-4o | deepseek-v4-pro |
| claude-sonnet | deepseek-v4-pro |
| gemini-2.5-pro | deepseek-v4-pro |
"@
          echo $summary >> $env:GITHUB_STEP_SUMMARY
          echo "DS2API=$ds2apiExe" >> $env:GITHUB_OUTPUT
          echo "TUNNEL=$tunnelUrl" >> $env:GITHUB_OUTPUT

      - name: Monitor
        shell: pwsh
        run: |
          $ds2apiExe = "${{ steps.setup.outputs.DS2API }}"
          $tunnelUrl = "${{ steps.setup.outputs.TUNNEL }}"
          
          $wsh = New-Object -ComObject WScript.Shell
          
          for ($i = 1; $i -le 350; $i++) {
              $wsh.SendKeys('{F15}')
              
              $d = if ($ds2apiExe -and (Get-Process ds2api -EA SilentlyContinue)) { "OK" } else { "N/A" }
              $ram = [math]::Round((Get-CimInstance Win32_OperatingSystem).FreePhysicalMemory / 1024, 1)
              
              Write-Host "[$i/350] RAM:${ram}MB | ds2api:$d | tunnel:$tunnelUrl" -ForegroundColor Cyan
              
              if ($ds2apiExe -and $d -eq "N/A") {
                  Start-Process -FilePath $ds2apiExe -WorkingDirectory (Split-Path $ds2apiExe) -WindowStyle Hidden
              }
              
              Start-Sleep -Seconds 60
          }
